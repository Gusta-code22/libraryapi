package io.github.Gusta_code22.libraryapi.controller.dto;

public record ErroCampo(String campo, String erro) { // campo de dentro do DTO principal com o campo ex: nome e o erro ex o campo nome nao esta preenchido
}
